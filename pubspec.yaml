name: mobile
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.12.0+2
  camera_web: ^0.3.2+4
  file_picker: ^11.0.3
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  latlong2: ^0.9.0
  geolocator: ^13.0.2
  flutter_map: ^7.0.2
  flutter_bloc: ^8.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
